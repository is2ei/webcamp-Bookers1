<h1>Editing Book</h1>

<%= form_with model: @book, url: book_path, local: true, class: "edit_book" do |f| %>
    <% if @book.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@book.errors.count, 'error') %> prohibited this book from being saved:</h2>
            <ul>
                <% @book.errors.full_messages.each do |msg| %>
                    <li><%= msg %>
                <% end %>
            </uL>
        </div>
    <% end %>
    <div class="field">
        <%= f.label :title, 'Title' %>
        <%= f.text_field :title %>
    </div>
    <div class="field">
        <%= f.label :body, 'Body' %>
        <%= f.text_area :body %>
    </div>
    <div class="actions">
        <%= submit_tag('Update Book') %>
    </div>
<% end %>

<%= link_to 'Show', book_path(@book.id) %> | <%= link_to 'Back', books_path %>
